# Assignment 2 - Dockerized Diagnostic CLI

A lightweight Dockerized CLI containerizing Linux system and network diagnostic scripts.

## Requirements
- Docker Desktop installed and running
- Docker Compose v2

## Usage

### Build Image
```bash
docker build -t diagnostic-cli:latest .
